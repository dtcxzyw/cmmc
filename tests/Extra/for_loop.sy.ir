internal func @putint() -> void { NoMemoryRead NoMemoryWrite };
func @main() -> i32 { NoMemoryRead NoMemoryWrite NoRecurse Entry } {
^entry:
    ubr ^for.header;
^for.header:
    i32 %0 = phi [^for.body, i32 %2] [^entry, i32 0];
    i1 %1 = scmp lt i32 %0, i32 10;
    cbr i1 %1(prob = 0.97), ^for.body, ^for.header1;
^for.body:
    call (i32) -> void @putint(i32 %0);
    i32 %2 = add i32 %0, i32 1;
    ubr ^for.header;
^for.header1:
    i32 %3 = phi [^for.header, i32 0] [^for.body1, i32 %5];
    i1 %4 = scmp lt i32 %3, i32 10;
    cbr i1 %4(prob = 0.97), ^for.body1, ^for.body2;
^for.body1:
    call (i32) -> void @putint(i32 %3);
    i32 %5 = add i32 %3, i32 1;
    ubr ^for.header1;
^for.body2:
    i32 %6 = phi [^for.header1, i32 0] [^b, i32 %7] [^for.body2, i32 %7];
    i32 %7 = add i32 %6, i32 1;
    i1 %8 = scmp eq i32 %6, i32 4;
    cbr i1 %8(prob = 0.5), ^for.body2, ^b;
^b:
    i1 %9 = scmp gt i32 %6, i32 8;
    cbr i1 %9(prob = 0.5), ^b1, ^for.body2;
^b1:
    ret i32 %7;
}
