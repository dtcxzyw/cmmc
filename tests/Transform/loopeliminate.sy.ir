internal func @putint() -> void { NoMemoryRead NoMemoryWrite };
func @main() -> i32 { NoMemoryRead NoMemoryWrite NoRecurse Entry } {
^entry:
    ubr ^for.header;
^for.header:
    i32 %0 = phi [^for.body, i32 %3] [^entry, i32 0];
    i32 %1 = phi [^for.body, i32 %3] [^entry, i32 0];
    i1 %2 = scmp lt i32 %1, i32 1;
    cbr i1 %2(prob = 0.97), ^for.body, ^for.header1;
^for.body:
    call (i32) -> void @putint(i32 %0);
    i32 %3 = add i32 %0, i32 1;
    ubr ^for.header;
^for.header1:
    i32 %4 = phi [^for.header, i32 -1] [^for.body1, i32 %7];
    i32 %5 = phi [^for.header, i32 -1] [^for.body1, i32 %7];
    i1 %6 = scmp lt i32 %5, i32 0;
    cbr i1 %6(prob = 0.97), ^for.body1, ^b;
^for.body1:
    call (i32) -> void @putint(i32 %4);
    i32 %7 = add i32 %4, i32 1;
    ubr ^for.header1;
^b:
    ret i32 0;
}
