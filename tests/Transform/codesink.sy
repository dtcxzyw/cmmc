int main() {
    int a = getint();
    int b = getint();
    int c = a * b + 1;
    int d = a * b + 2;
    int e = a * b + 3;
    int sum = a * b;
    for(int i = 0; i < 1000; ++i)
        sum += i;
    putint(sum);
    putint(e);
    putint(d);
    putint(c);
    return 0;
}
