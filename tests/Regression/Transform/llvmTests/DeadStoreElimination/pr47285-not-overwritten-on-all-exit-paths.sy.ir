func @test(i32 %var0, i32 %var1, i32 %var2, i32 %var3, i32 %var4, i32 %var5) -> void { NoMemoryRead NoRecurse } {
  ^entry:
    i1 %0 = scmp neq i32 %var0, i32 0;
    ubr ^block1;
  ^block1:
    cbr i1 %0(prob = 0.5), ^if.then, ^b;
  ^if.then:
    i1 %1 = scmp neq i32 %var1, i32 0;
    cbr i1 %1(prob = 0.5), ^if.then1, ^if.else;
  ^b:
    ret;
  ^if.then1:
    i1 %2 = scmp neq i32 %var3, i32 0;
    cbr i1 %2(prob = 0.5), ^indirect, ^if.else1;
  ^if.else:
    i1 %3 = scmp neq i32 %var3, i32 0;
    cbr i1 %3(prob = 0.5), ^if.then2, ^if.else2;
  ^indirect:
    i1 %4 = scmp neq i32 %var2, i32 0;
    ubr ^block5;
  ^if.else1:
    i32* %5 = ptrcast i32* @b to i32*;
    store i32* %5 with i32 91;
    i1 %6 = scmp neq i32 %var4, i32 0;
    cbr i1 %6(prob = 0.5), ^if.then3, ^block1;
  ^if.then2:
    i32* %7 = ptrcast i32* @b to i32*;
    store i32* %7 with i32 9;
    i1 %8 = scmp neq i32 %var5, i32 0;
    cbr i1 %8(prob = 0.5), ^if.then4, ^block1;
  ^if.else2:
    i32* %9 = ptrcast i32* @b to i32*;
    store i32* %9 with i32 19;
    cbr i1 %3(prob = 0.5), ^b, ^block1;
  ^if.then3:
    i32* %10 = ptrcast i32* @b to i32*;
    store i32* %10 with i32 27;
    ubr ^b;
  ^if.then4:
    i32* %11 = ptrcast i32* @b to i32*;
    store i32* %11 with i32 23;
    ubr ^b;
  ^block5:
    i32* %12 = ptrcast i32* @b to i32*;
    store i32* %12 with i32 99;
    cbr i1 %4(prob = 0.5), ^block5, ^block1;
}
i32* @b = 0;
