func @baz() -> i32 { NoMemoryRead NoMemoryWrite NoSideEffect Stateless NoRecurse } {
  ^entry:
    ret i32 0;
}
func @runtime(i32 %var0) -> i32 { NoMemoryRead NoMemoryWrite NoSideEffect Stateless NoRecurse } {
  ^entry:
    i1 %0 = icmp eq i32 %var0, i32 1;
    cbr i1 %0(prob = 0.5), ^if.then, ^if.else;
  ^if.else:
    i32 %1 = phi [^entry, i32 1] [^if.else, i32 %5];
    i32 %2 = phi [^entry, i32 0] [^if.else, i32 %7];
    i32 %3 = strunc i32 %1 to i32;
    i32 %4 = add i32 %3, i32 1;
    i32 %5 = ztrunc i32 %4 to i32;
    i1 %6 = icmp eq i32 %var0, i32 %5;
    i32 %7 = ztrunc i32 %2 to i32;
    cbr i1 %6(prob = 0.5), ^if.then, ^if.else;
  ^if.then:
    i32 %8 = phi [^entry, i32 0] [^if.else, i32 %7];
    i32 %9 = ztrunc i32 %8 to i32;
    ret i32 %9;
}
func @sum() -> i32 { NoMemoryRead NoMemoryWrite NoSideEffect Stateless NoRecurse } {
  ^b:
    ret i32 0;
}
