int main() {
    int a = getint();
    int b = getint();
    if(a && b) {
        putint(1);
    }
    if(a || b) {
        putint(2);
    }
    return 0;
}
int select_bitset(int x, int y, int z) {
    return (x == 0 && y == 1) ? z : 0;
}
