int test1(int x, int y) {
    int a = x == 0;
    int b = y == 0;
    return (a || b) && (a || !b) && (!a || b) && (!a || !b);
}
int test2(int x) {
    int a = x == 0;
    return a || !a;
}
int test3(int x, int y) {
    return x < y && x > y;
}
int test4(int x, int y) {
    return x <= y || x >= y;
}
