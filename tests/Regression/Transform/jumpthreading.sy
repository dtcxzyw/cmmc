int main() {
    int x = 4;
    if(getint()) {
        x = 3;
    }
    if(x < 4) {
        putint(getint());
    }
    else {
        putint(0);
    }
    return 0;
}
void test(int x[], int n, int z) {
    for(int i = 0; i < n; ++i)
        if(z)
            x[i] = z;
}
