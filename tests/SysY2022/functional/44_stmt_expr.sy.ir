internal func @putint() -> void { NoMemoryRead NoMemoryWrite };
func @main() -> i32 { NoMemoryRead NoMemoryWrite NoRecurse Entry } {
^entry:
    ubr ^while.header;
^while.header:
    i32 %0 = phi [^while.body, i32 %4] [^entry, i32 0];
    i32 %1 = phi [^while.body, i32 %5] [^entry, i32 1];
    i32 %2 = phi [^while.body, i32 %4] [^entry, i32 0];
    i1 %3 = scmp lt i32 %2, i32 10;
    cbr i1 %3(prob = 0.97), ^while.body, ^b;
^while.body:
    i32 %4 = add i32 %0, i32 1;
    i32 %5 = mul i32 %1, i32 2;
    ubr ^while.header;
^b:
    call (i32) -> void @putint(i32 %1);
    ret i32 %1;
}
